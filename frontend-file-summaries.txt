{
  "frontend/src/index.css": "Global styles with OKLCH color system — `:root` now uses the same deep charcoal dark values as `.dark` so the app always renders with a black/near-black background regardless of system theme; all other design tokens (gold, silver, typography, component utilities) remain unchanged.",
  "frontend/tailwind.config.js": "Extended Tailwind config with custom fontFamily (Playfair Display serif, Inter sans), gold/silver/charcoal color tokens mapped to OKLCH CSS variables including explicit numeric shades (charcoal-600 through charcoal-950, gold-400/500, silver-300 through silver-600) used by page components, custom box shadows, letter spacing, and fade-in animations.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations including user role fetching, Stripe configuration check, Stripe configuration mutation, and checkout session creation with proper JSON parsing and URL validation.",
  "frontend/src/App.tsx": "Root app component that initializes TanStack Router with the route tree and renders RouterProvider.",
  "frontend/src/routeTree.ts": "TanStack Router route tree defining all six routes; the `/payment-success` route now includes `validateSearch` to parse and default the `quantity` search parameter, matching the pattern used by `/checkout`.",
  "frontend/src/components/Layout.tsx": "Shared layout with fixed dark header, mobile hamburger menu with full-width touch-friendly nav items (min-height 48px), responsive padding, and caffeine.ai footer attribution.",
  "frontend/src/pages/LoginPage.tsx": "Login page with responsive hero image height (h-48 on mobile, full on desktop), touch-friendly sign-in button (min-height 52px), and adjusted spacing for small screens.",
  "frontend/src/pages/ProductLandingPage.tsx": "Mobile-responsive product landing page with hero, feature highlights (AI detection, steam control, garment protection), product showcase, lifestyle, and pricing sections using only user-provided images; all robotic arm mentions removed and copy updated to read naturally.",
  "frontend/src/pages/FeaturesPage.tsx": "Features page with 6 feature cards, technical specs table, and a lifestyle CTA section that includes a touch-friendly quantity selector (increment/decrement + numeric input, min 1, default 1) with live price calculation, positioned before the \"Order Now\" button; navigates to /checkout with quantity as a search param. All robotic arm mentions removed.",
  "frontend/src/pages/CheckoutPage.tsx": "Three-step checkout (contact → method → confirm) reading quantity from URL search params; the `handlePay` function now passes `quantity` as a search param when navigating to `/payment-success` so the correct quantity flows through to the confirmation page.",
  "frontend/src/pages/PaymentSuccess.tsx": "Payment success confirmation page that now reads `quantity` from URL search params (defaulting to 1), calculates `totalPrice = quantity × ₹20,000`, and displays the actual quantity and correct total in the product summary section.",
  "frontend/src/pages/PaymentFailure.tsx": "Payment failure page showing cancellation message, common reasons list, and retry/back actions; the \"Try Again\" button now navigates to /checkout with the required search param (quantity: '1') to satisfy the route's validateSearch constraint.",
  "frontend/index.html": "HTML entry point with updated page title \"AIron - Automatic Ironing Machine\"."
}